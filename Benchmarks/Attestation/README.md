# Attestation report generation analysis scripts

`results.csv` and `crafted_results.csv` currently have some sample results for the attestation report generation measurements.

To produce the plots run:
```
python3 plot.py <result_file>
```

The plots will be stored in the `output` directory.