# Simulation analysis scripts

`results.txt` file currently has some sample results for the VM, CVM and Wallet variants.

To produce the plots run:
```
python3 plot.py results.txt
```

The plots will be stored in the `output` directory.