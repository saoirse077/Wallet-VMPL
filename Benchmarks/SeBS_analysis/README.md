# SeBS analysis scripts

`results` directory currently has some sample results for some of the variants.

To produce the plots run:
```
python3 plot.py
```

The plots will be stored in the `output` directory.