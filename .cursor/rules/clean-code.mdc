---
description: Clean code and architecture standards - enforce clean code principles, reduce redundancy, and maintain code quality
alwaysApply: true
---

# Clean Code 规范

## 函数设计

- 每个函数不超过 100 行，超出时应拆分为更小的子函数
- 遵循单一职责原则（SRP）：每个函数只做一件事，且做好
- 函数参数尽量控制在 3 个以内，过多时考虑使用结构体封装

## 减少冗余

- 消除重复代码，提取公共逻辑为独立函数或模块
- 避免复制粘贴式编程，遵循 DRY 原则（Don't Repeat Yourself）
- 不保留被注释掉的死代码，依赖版本控制追溯历史

## 代码坏味道识别与改进

主动识别并改进以下常见问题：

- **过长函数**：拆分为职责单一的小函数
- **过大的类/模块**：按职责拆分为多个模块
- **过深的嵌套**：使用 early return、guard clause 减少嵌套层级
- **魔法数字/字符串**：提取为命名常量或枚举
- **过长的参数列表**：封装为结构体或配置对象
- **重复代码**：提取为公共函数
- **含糊的命名**：使用清晰、有意义的变量和函数名

## 命名规范

- 变量和函数名应清晰表达意图，避免缩写（除非是领域内通用缩写）
- 布尔变量使用 `is_`、`has_`、`can_` 等前缀
- 函数名使用动词开头，表达其行为

## 架构整洁

- 保持模块间低耦合、高内聚
- 明确模块职责边界，避免循环依赖
- 对外暴露清晰的接口，隐藏内部实现细节
