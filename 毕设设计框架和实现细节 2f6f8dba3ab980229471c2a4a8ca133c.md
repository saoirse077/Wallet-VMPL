# 毕设设计框架和实现细节

# 框架设计

**设计并实现一种面向机密 Serverless 的轻量级安全隔离与远程认证机制：**
以 WebAssembly 作为函数运行时（WAMR)，在 AMD SEV-SNP 的 VMPL 架构下，利用 MPK/PKRU 实现同一 VM 内多 Serverless 函数的强隔离。一个Serverless函数在一个Wasm Module中，不同wasm module由mpk隔离开，由可信监控器（VMPL0）对运行时与函数模块隔离进行集中管理和远程认证。远程认证包括对可信监控器monitor、wamr运行时、wasm模块、serverless函数、invoke serverless的输入输出。

## 1️⃣ VMPL 分层设计

系统在逻辑上划分为四个信任层次：

- **VMPL0（Trusted Monitor）**：负责内存分配、RMP 与页表配置、MPK 管理以及远程认证；
- **VMPL1（Serverless Execution Environment）**：运行 WebAssembly runtime 与函数模块，执行计算逻辑；
- **VMPL2（Guest OS）**：提供传统操作系统服务，如文件系统与设备 I/O；
- **Hypervisor**：不可信，仅负责虚拟化。

你的系统严格基于 **SEV-SNP 的 VMPL 分层模型**：

| 层级 | 角色 | 可信性 | 主要职责 |
| --- | --- | --- | --- |
| **VMPL0** | 可信 Monitor（SVSM-like） | 信任 | 内存分配、RMP/页表管理、MPK 管理、远程认证 |
| **VMPL1** | Serverless 执行环境 | 信任 | WebAssembly Runtime + 函数模块（实例化） |
| **VMPL2** | Guest OS | 不信任 | 文件读取、I/O等（处理经由可行Monitor转发来的协助request) |
| **Hypervisor** | 不可信 | 不信任 | 仅负责虚拟化，不参与信任 |

## 2️⃣ serverless函数安全隔离模型（和 Wallet 的关键差异）

多个函数可共享同一个 WebAssembly runtime，但每个函数模块实例对应一个线程（一个vcpu、一个pkey)，拥有独立的 WebAssembly 线性内存（线性字节地址空间）。在 VMPL0的可信monitor中为这些serverlss函数(线程）分配不同的 protection key，并通过对pkru寄存器、Pkey位图、页表 Pkey 标记的严格管理实现不同serverlss函数间的硬件级访问隔离。

- **Wallet**：
    - 一个 Serverless 函数 ≈ 一个 LibOS（Gramine），通过vpml0管理可信进程页表，页表隔离
    - 运行 Python runtime
- **我的方案**：
    - 一个 Serverless 函数 ≈ 一个 **WASM 模块实例（一个vcpu只跑一个线程，对应一个函数），**不同函数模块和实例通过pkey隔离，pkey的管理放在vmpl0中。
    - 多个函数可共享 同一个 WASM runtime



# VMPL0 可信 Monitor 的职责设计

**明确继承Wallet论文中 PAL → Monitor 的思想，通过CPUID由VMPL1将关键操作切换到VMPL0由可信monitor执行**

## 1 内存管理

VMPL1 中的 WebAssembly runtime 不直接执行内存管理相关的系统调用。所有内存分配与释放请求的“系统调用”通过提交**PAL和CPUID**提交至 VMPL0。可信监控器在 VMPL0 层完成以下操作：

- 分配物理内存页；
- 使用 **RMPADJUST** 指令配置页面的 VMPL 访问权限；
- 建立或更新页表映射；
- 为不同函数分配并绑定对应的 MPK protection key。

## 2 MPK 权限控制

为防止 VMPL1 中的代码滥用 WRPKRU 指令，本设计不允许 WebAssembly runtime 或函数代码直接修改 PKRU 寄存器。对PKRU寄存器、Pkey位图、页表 Pkey 标记等mpk操作通过PAL-like 接口请求 VMPL0 执行，由可信监控器在完成校验后进行操作管理，从而实现集中式的权限管理。

- **VMPL1 中不允许任意 WRPKRU**
- 除RDPKRU外的其他mpk相关操作均需通过CPUID trap到VMPL0处理
- 

### **VMPL0功能细节设计：**

1. 维护pkey位图（操作时需加锁）

 3.   VMPL1中**新进程/线程的 PKRU 默认禁止 pkey 1-15，只允许 pkey 0**

如VMPL-1 启动时，初始化 PKRU = 0x55555554                                  
     - pkey 0 允许（给 VMPL-1 Guest OS 自己用）                          
     - pkey 1-15 禁止（给 WASM 模块用）

### **VMPL0为VMPL1提供的接口：**

- 分配pkey：在Pkey位图中查到一个空闲的pkey返回
- 申请内存接口（参数：vaddr起始虚拟地址，size大小，pkey，*PalProtFlags（可选，看需不需要））：*分配内存，建立页表映射，给页表的均打上Pkey 标记。
- 进入安全域/打开线程PKRU寄存器上Pkey权限位（参数：pkey，…)：
    - 安全策略：首先检查除了pkey0的权限位外，是否还有别的pkey的权限位已经打开（不是禁止读禁止写了），如果有别的pkey的权限位已经打开，则拒绝本次操作。
    - 如果只有pkey0的权限位是开的，则打开参数传递的pkey的权限位为可读可写。
- 退出域/关上对应pkey权限（参数：pkey，…)：关上线程对应pkey权限（禁止读禁止写）
- 释放内存（参数：vaddr起始虚拟地址，size大小*）*：先清空内存，遍历页表去掉页表上的pkey标志。
- 释放pkey（参数：vaddr起始虚拟地址，size大小，Pkey）：清空内存（如果vaddr和size不为0）；遍历页表去掉页表上的pkey标志（如果vaddr和size不为0）；关上线程对应pkey权限（禁止读禁止写（如果已经关闭就不再操作了）；在pkey位图中恢复对应Pkey可用。

**以上六个接口也是目前要在vmpl0的可信monitor(svsm)中开发实现的功能。**

## 3 操作系统服务访问

在本设计中，**VMPL1 不直接执行 syscall 指令**。所有对操作系统服务的访问均通过 WebAssembly 的 **WASI host functions** 发起，**并被重定向至 PAL-like 接口，通过CPUID转到VMPL0**。可信监控器在 VMPL0 层根据请求类型执行如下处理：

- 对于内存管理等安全关键操作，由 VMPL0 直接处理；
- 对于文件访问与设备 I/O 等操作，VMPL0 将请求显式转发给运行在 VMPL2 的 Guest OS 进行处理，并将结果返回给 VMPL1。

[Wallet运行环境](https://www.notion.so/Wallet-2faf8dba3ab9806f805bccd3b1a81165?pvs=21)